Git does not track empty directories, but conf.py names _static in
html_static_path and Sphinx warns when it is missing -- which -W turns into a
failed build in any fresh checkout. This file keeps the directory tracked.
